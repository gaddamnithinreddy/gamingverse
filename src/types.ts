export interface Game {
  id: number;
  title: string;
  shortDescription: string;
  description: string;
  image: string;
  playUrl: string;
  genre: string;
  platform: string;
  features: string[];
}